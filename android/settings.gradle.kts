pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("com.android.library") version "8.9.1" apply false
}

rootProject.name = "ffi_jpeg_encode"
