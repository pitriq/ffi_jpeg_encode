name: ffi_jpeg_encode
description: "Fast JPEG encoding via FFI with SIMD optimizations for Android and iOS."
version: 1.0.0
homepage: https://github.com/pitriq/ffi_jpeg_encode

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4
  hooks: ^1.0.0
  code_assets: ^1.0.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
